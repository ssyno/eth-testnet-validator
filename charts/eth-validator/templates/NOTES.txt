{{ .Chart.Name }} deployed to {{ .Values.network | title }}.

Check status:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

View logs:
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=execution -f
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=consensus -f
  kubectl logs -n {{ .Release.Namespace }} -l app.kubernetes.io/component=validator -f

Required secrets:
  - {{ .Values.jwtSecret.existingSecretName }} (key: {{ .Values.jwtSecret.key }})
  - {{ .Values.validatorKeys.existingSecretName }} (validator_keys.tar.gz, validator_password.txt)

WARNING: Never run multiple validators with the same keys - slashing risk!
